<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".CameraActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textSize="25sp"
        android:textColor="@color/design_blue"
        android:id="@+id/activity_camera_tv"
        android:layout_margin="2dp"
        android:padding="4dp"
        android:textAlignment="center"
        android:text="Tap to Open Camera"
        />

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        app:layout_constraintLeft_toLeftOf="@id/activity_camera_tv"
        app:layout_constraintTop_toBottomOf="@id/activity_camera_tv"
        app:layout_constraintRight_toRightOf="@id/activity_camera_tv"
        android:layout_margin="8dp"
        android:id="@+id/activity_camera_logo"
        android:src="@drawable/baseline_upload_file_24"
        app:tint="@color/design_blue" />

    <ImageView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/activity_camera_tv"
        android:src="@drawable/baseline_insert_photo_24"
        android:layout_margin="@dimen/glass_card_margin"
        android:id="@+id/activity_camera_imageview"/>

</androidx.constraintlayout.widget.ConstraintLayout>